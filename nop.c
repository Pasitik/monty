#include "monty.h"

/**
 * nop - does nothing
 * @stack: Pointer to the stack.
 * @line_no: Line number where the nop function is called.
 */

void nop(stack_t **stack, unsigned int line_no)
{
	(void) stack;
	(void) line_no;
}
